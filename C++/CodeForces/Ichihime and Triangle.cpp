#include<bits/stdc++.h>
using namespace std;
int main() {
    ios_base::sync_with_stdio(false);
    int t,a,b,c,d;
    cin >> t;
    while(t-- > 0) {
        cin >> a >> b >> c >> d;
        cout << b << " " << c << " " << c << "\n";
    }
}
